import { expect } from "chai"

import { PuzzleGeneratorService } from "../../generateSudokuGrid/puzzlegeneratorservice";

describe("Puzzle Generator Service", () => {
    it("", () => {
        const testService: PuzzleGeneratorService = new PuzzleGeneratorService();
        expect().equals();
        expect().equals();
    });
})